"""
Common package — shared infrastructure for all modules.

Exports:
    database  — async engine, session factory, lifecycle hooks
    logger    — structured JSON logging factory
    exceptions — typed exception hierarchy
    response  — standardised API response envelope
    utils     — pure utility functions
"""
